// Kenny Oseleononmen
// koseleo1

#include "dnasearch.h"
#include <stdio.h>
#include <string.h>

int main()
{

    int pattern_size = 0;
    char* user_pattern = malloc(15001 * sizeof(char));
    char current_char;
    user_pattern++;
    while (scanf("%c", &current_char) != EOF)
    {
        switch(toupper(current_char))
        {
            case 'A':
            case 'C':
            case 'G':
            case 'T':
                user_pattern[]

        }
    }
}